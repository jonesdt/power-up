- name: Get route to client
  command: "{{ hostvars['localhost']['python_executable_local'] }} \
  {{ hostvars['localhost']['scripts_path_local'] }}/python/ip_route_get_to.py \
  {{ inventory_hostname }}"
  delegate_to: localhost
  register: host_ip

- name: ENGR_MODE Playbook Pause
  command: "{{ hostvars['localhost']['scripts_path_local'] }}/python/ENGR_MODE_PAUSE.py" #Create pause python function under scripts directory
